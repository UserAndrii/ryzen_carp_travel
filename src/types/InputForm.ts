import { FormDataOptions } from './FormDataOptions';

export type InputForm = {
  id: string;
  name: FormDataOptions;
  type: string;
  autoComplete: string;
  placeholder: string;
  label: string;
};
